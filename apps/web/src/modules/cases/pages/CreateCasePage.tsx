import { useState } from 'react';import { Link } from 'react-router-dom';import { useForm } from 'react-hook-form';import { zodResolver } from '@hookform/resolvers/zod';import { z } from 'zod';import { useCreateMatter } from '../hooks/useMatters';import { useClients, useCreateClient } from '../../clients/hooks/useClients';import { useVocabulary } from '../../../shared/hooks/useVocabulary';import type { CreateMatterDto, CreateClientDto } from '@dsx/shared';const createMatterSchema = z.object({  title: z.string().min(1, 'Title is required').max(200, 'Title too long'),  internalRef: z.string().min(1, 'Internal ref is required').max(50, 'Ref too long').regex(/^[A-Za-z0-9\-\/]+$/, 'Only letters, numbers, hyphens and slashes allowed'),  externalRef: z.string().max(50, 'Ref too long').optional(),  participantId: z.string().min(1, 'Please select a client'),  statusKey: z.string().min(1, 'Status is required'),  metadata: z.record(z.string(), z.unknown()).optional(),});const createClientSchema = z.object({  name: z.string().min(1, 'Name is required').max(200),  email: z.string().optional(),  phone: z.string().max(20).optional(),});type CreateMatterForm = z.infer<typeof createMatterSchema>;type CreateClientForm = z.infer<typeof createClientSchema>;const INPUT_CLS =  'block w-full rounded-lg border border-slate-300 px-3.5 py-2.5 text-sm text-slate-900 placeholder-slate-400 shadow-sm focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-500/20';const LABEL_CLS = 'block text-sm font-medium text-slate-700 mb-1.5';export function CreateCasePage() {  const vocab = useVocabulary();  const { mutate: createMatter, isPending, error } = useCreateMatter();  const { data: clients = [], isLoading: clientsLoading } = useClients();  const { mutate: createClient, isPending: creatingClient } = useCreateClient();  const [showNewClient, setShowNewClient] = useState(false);  const [newClientError, setNewClientError] = useState<string | null>(null);  const {    register,    handleSubmit,    setValue,    watch,    formState: { errors },  } = useForm<CreateMatterForm>({    resolver: zodResolver(createMatterSchema),    defaultValues: { statusKey: vocab.statuses[0]?.key ?? 'filed' },  });  const selectedParticipantId = watch('participantId');  const {    register: registerClient,    handleSubmit: handleClientSubmit,    reset: resetClientForm,    formState: { errors: clientErrors },  } = useForm<CreateClientForm>({    resolver: zodResolver(createClientSchema),  });  const onSubmit = (data: CreateMatterForm) => {    createMatter(data as CreateMatterDto);  };  const onCreateClient = (data: CreateClientForm) => {    setNewClientError(null);    createClient(data as CreateClientDto, {      onSuccess: (created) => {        setValue('participantId', created.id);        setShowNewClient(false);        resetClientForm();      },      onError: () => {        setNewClientError('Failed to create client. Check details and try again.');      },    });  };  return (    <div className="px-6 py-8 max-w-2xl mx-auto">      {/* Page header */}      <div className="flex items-center gap-2 mb-6 text-sm text-slate-500">        <Link to="/cases" className="hover:text-indigo-600 transition-colors">{vocab.matter_plural}</Link>        <svg width="14" height="14" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>          <path strokeLinecap="round" strokeLinejoin="round" d="M9 5l7 7-7 7" />        </svg>        <span className="text-slate-800 font-medium">New {vocab.matter_label}</span>      </div>      <div className="rounded-xl border border-slate-200 bg-white shadow-sm overflow-hidden">        {/* Card header */}        <div className="px-6 py-5 border-b border-slate-100">          <h1 className="text-lg font-semibold text-slate-900">New {vocab.matter_label}</h1>          <p className="mt-0.5 text-sm text-slate-500">Fill in the details to open a new {vocab.matter_label.toLowerCase()} file.</p>        </div>        <form onSubmit={handleSubmit(onSubmit)} className="px-6 py-6 space-y-5">          {/* Title */}          <div>            <label className={LABEL_CLS}>              Title <span className="text-red-500">*</span>            </label>            <input              type="text"              placeholder="e.g. Smith vs State"              className={INPUT_CLS}              {...register('title')}            />            {errors.title && <p className="mt-1.5 text-xs text-red-500">{errors.title.message}</p>}          </div>          {/* Internal Ref + External Ref */}          <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">            <div>              <label className={LABEL_CLS}>                Internal Ref <span className="text-red-500">*</span>              </label>              <input                type="text"                placeholder="e.g. NA-2024-001"                className={INPUT_CLS}                {...register('internalRef')}              />              {errors.internalRef && (                <p className="mt-1.5 text-xs text-red-500">{errors.internalRef.message}</p>              )}            </div>            <div>              <label className={LABEL_CLS}>Court Ref</label>              <input                type="text"                placeholder="Court case number"                className={INPUT_CLS}                {...register('externalRef')}              />            </div>          </div>          {/* Status */}          <div>            <label className={LABEL_CLS}>              Status <span className="text-red-500">*</span>            </label>            <select              className={INPUT_CLS}              {...register('statusKey')}            >              {vocab.statuses.map((s) => (                <option key={s.key} value={s.key} disabled={s.isTerminal}>                  {s.label}                </option>              ))}            </select>          </div>          {/* Client picker */}          <div>            <label className={LABEL_CLS}>              {vocab.participant_label} <span className="text-red-500">*</span>            </label>            <select              className={INPUT_CLS}              value={selectedParticipantId ?? ''}              onChange={(e) => {                const val = e.target.value;                if (val === '__new__') {                  setShowNewClient(true);                  setValue('participantId', '');                } else {                  setShowNewClient(false);                  setValue('participantId', val);                }              }}              disabled={clientsLoading}            >              <option value="">                {clientsLoading ? 'Loading…' : `Select ${vocab.participant_label}`}              </option>              {clients.map((c) => (                <option key={c.id} value={c.id}>                  {c.name}                  {c.email ? ` — ${c.email}` : ''}                  {c.phone ? ` — ${c.phone}` : ''}                </option>              ))}              <option value="__new__">+ Add new {vocab.participant_label}</option>            </select>            <input              type="hidden"              {...register('participantId')}            />            {errors.participantId && (              <p className="mt-1.5 text-xs text-red-500">{errors.participantId.message}</p>            )}            {/* Inline new-client mini-form */}            {showNewClient && (              <div className="mt-3 rounded-lg border border-indigo-200 bg-indigo-50 p-4 space-y-3">                <p className="text-sm font-semibold text-indigo-800">                  New {vocab.participant_label}                </p>                <div>                  <label className="block text-xs font-medium text-slate-700 mb-1">                    Name <span className="text-red-500">*</span>                  </label>                  <input                    type="text"                    placeholder="Full name"                    className={INPUT_CLS}                    {...registerClient('name', { required: 'Name is required' })}                  />                  {clientErrors.name && (                    <p className="mt-1 text-xs text-red-500">{clientErrors.name.message}</p>                  )}                </div>                <div className="grid grid-cols-2 gap-3">                  <div>                    <label className="block text-xs font-medium text-slate-700 mb-1">Email</label>                    <input                      type="email"                      placeholder="client@example.com"                      className={INPUT_CLS}                      {...registerClient('email')}                    />                  </div>                  <div>                    <label className="block text-xs font-medium text-slate-700 mb-1">Phone</label>                    <input                      type="tel"                      placeholder="+91 98765 43210"                      className={INPUT_CLS}                      {...registerClient('phone')}                    />                  </div>                </div>                {newClientError && (                  <p className="text-xs text-red-600">{newClientError}</p>                )}                <div className="flex gap-2">                  <button                    type="button"                    disabled={creatingClient}                    onClick={handleClientSubmit(onCreateClient)}                    className="rounded-lg bg-indigo-600 px-4 py-2 text-xs font-semibold text-white hover:bg-indigo-700 disabled:opacity-50 transition-colors"                  >                    {creatingClient ? 'Saving…' : 'Save Client'}                  </button>                  <button                    type="button"                    onClick={() => {                      setShowNewClient(false);                      resetClientForm();                      setNewClientError(null);                    }}                    className="rounded-lg border border-slate-300 px-4 py-2 text-xs font-medium text-slate-700 hover:bg-white transition-colors"                  >                    Cancel                  </button>                </div>              </div>            )}          </div>          {error && (            <div className="flex items-center gap-2 rounded-lg bg-red-50 border border-red-200 px-3.5 py-2.5">              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#ef4444" strokeWidth={2}>                <circle cx="12" cy="12" r="10" />                <path strokeLinecap="round" d="M12 8v4m0 4h.01" />              </svg>              <p className="text-sm text-red-600">Failed to create {vocab.matter_label.toLowerCase()}. Please try again.</p>            </div>          )}          {/* Actions */}          <div className="flex gap-3 pt-2 border-t border-slate-100">            <button              type="submit"              disabled={isPending || showNewClient}              className="rounded-lg bg-indigo-600 px-5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-indigo-700 disabled:opacity-50 transition-colors"            >              {isPending ? 'Creating…' : `Create ${vocab.matter_label}`}            </button>            <Link              to="/cases"              className="rounded-lg border border-slate-300 px-5 py-2.5 text-sm font-medium text-slate-700 hover:bg-slate-50 transition-colors"            >              Cancel            </Link>          </div>        </form>      </div>    </div>  );}
