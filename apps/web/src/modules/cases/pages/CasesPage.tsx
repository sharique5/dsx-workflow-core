import { useState, useMemo } from 'react';import { Link } from 'react-router-dom';import { useMatters } from '../hooks/useMatters';import { useVocabulary } from '../../../shared/hooks/useVocabulary';import { CasesTableSkeleton } from '../../../shared/components/Skeleton';import type { MatterDto } from '@dsx/shared';import { useStates } from '../hooks/useCourts';import { usePageTitle } from '../../../shared/hooks/usePageTitle';const PAGE_LIMIT = 25;function StatusBadge({ statusKey, statuses }: { statusKey: string; statuses: { key: string; label: string; isTerminal: boolean }[] }) {  const status = statuses.find((s) => s.key === statusKey);  const label = status?.label ?? statusKey;  const isTerminal = status?.isTerminal ?? false;  return (    <span      className={`inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium ${        isTerminal          ? 'bg-slate-100 text-slate-500'          : 'bg-indigo-50 text-indigo-700 border border-indigo-100'      }`}    >      {!isTerminal && (        <span className="mr-1.5 h-1.5 w-1.5 rounded-full bg-indigo-500" />      )}      {label}    </span>  );}function MatterRow({ matter, vocab }: { matter: MatterDto; vocab: ReturnType<typeof useVocabulary> }) {  return (    <tr className="hover:bg-slate-50 transition-colors group">      <td className="px-5 py-3.5 text-sm font-mono font-medium text-slate-700">        <Link to={`/cases/${matter.id}`} className="hover:text-indigo-600">          {matter.internalRef}        </Link>      </td>      <td className="px-5 py-3.5 text-sm text-slate-800 font-medium">        <Link to={`/cases/${matter.id}`} className="hover:text-indigo-600">          {matter.title}        </Link>      </td>      <td className="px-5 py-3.5 text-sm text-slate-500">        {matter.participant?.name ?? <span className="text-slate-300">—</span>}      </td>      <td className="px-5 py-3.5 text-sm">        <StatusBadge statusKey={matter.statusKey} statuses={vocab.statuses} />      </td>      <td className="px-5 py-3.5 text-sm text-slate-400">        {new Date(matter.createdAt).toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric' })}      </td>      <td className="px-5 py-3.5 text-sm text-right">        <Link          to={`/cases/${matter.id}`}          className="text-slate-400 hover:text-indigo-600 opacity-0 group-hover:opacity-100 transition-opacity"        >          <svg width="16" height="16" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>            <path strokeLinecap="round" strokeLinejoin="round" d="M9 5l7 7-7 7" />          </svg>        </Link>      </td>    </tr>  );}export function CasesPage() {  usePageTitle('Cases');  const vocab = useVocabulary();  const [page, setPage] = useState(1);  const { data: paged, isLoading, isError } = useMatters(page, PAGE_LIMIT);  const matters = paged?.data;  const total = paged?.total ?? 0;  const totalPages = Math.ceil(total / PAGE_LIMIT);  const [search, setSearch] = useState('');  const [statusFilter, setStatusFilter] = useState('');  const [stateFilter, setStateFilter] = useState('');  const { data: states = [], isLoading: statesLoading } = useStates();  const filtered = useMemo(() => {    if (!matters) return [];    const q = search.trim().toLowerCase();    return matters.filter((m) => {      const matchesSearch =        !q ||        m.internalRef.toLowerCase().includes(q) ||        m.title.toLowerCase().includes(q) ||        (m.participant?.name ?? '').toLowerCase().includes(q) ||        (m.externalRef ?? '').toLowerCase().includes(q);      const matchesStatus = !statusFilter || m.statusKey === statusFilter;      const matchesState = !stateFilter || m.metadata['state'] === stateFilter;      return matchesSearch && matchesStatus && matchesState;    });  }, [matters, search, statusFilter]);  return (    <div className="px-6 py-8 max-w-7xl mx-auto">      {/* Page header */}      <div className="flex items-center justify-between mb-6">        <div>          <h1 className="text-2xl font-bold text-slate-900">{vocab.matter_plural}</h1>          {matters && (            <p className="mt-0.5 text-sm text-slate-500">              {total} {total === 1 ? vocab.matter_label.toLowerCase() : vocab.matter_plural.toLowerCase()} total            </p>          )}        </div>        <Link          to="/cases/new"          className="inline-flex items-center gap-2 rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-indigo-700 transition-colors"        >          <svg width="14" height="14" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2.5}>            <path strokeLinecap="round" strokeLinejoin="round" d="M12 4v16m8-8H4" />          </svg>          New {vocab.matter_label}        </Link>      </div>      {/* Search + filter bar */}      {matters && matters.length > 0 && (        <div className="mb-4 flex flex-col sm:flex-row gap-3">
          <div className="relative flex-1">            <svg className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" width="14" height="14" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>              <path strokeLinecap="round" strokeLinejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />            </svg>            <input              type="text"              value={search}              onChange={(e) => setSearch(e.target.value)}              placeholder={`Search ${vocab.matter_plural.toLowerCase()}…`}              className="w-full rounded-lg border border-slate-300 pl-9 pr-3.5 py-2.5 text-sm text-slate-900 placeholder-slate-400 focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-500/20"            />          </div>          <select            value={statusFilter}            onChange={(e) => setStatusFilter(e.target.value)}            className="rounded-lg border border-slate-300 px-3 py-2.5 text-sm text-slate-700 focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-500/20 bg-white"          >            <option value="">All statuses</option>            {vocab.statuses.map((s) => (              <option key={s.key} value={s.key}>{s.label}</option>            ))}          </select>          <div className="relative">            <select              value={stateFilter}              onChange={(e) => setStateFilter(e.target.value)}              className={`rounded-lg border border-slate-300 px-3 py-2.5 text-sm text-slate-700 focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-500/20 bg-white${statesLoading ? ' opacity-50 cursor-not-allowed' : ''}`}              disabled={statesLoading}            >              <option value="">All states</option>              {states.map((s) => (                <option key={s.id} value={s.name}>{s.name}</option>              ))}            </select>            {statesLoading && (              <span className="pointer-events-none absolute inset-y-0 right-8 flex items-center">                <svg className="animate-spin h-4 w-4 text-slate-400" viewBox="0 0 24 24" fill="none">                  <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />                  <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z" />                </svg>              </span>            )}          </div>          {(search || statusFilter || stateFilter) && (            <button              onClick={() => { setSearch(''); setStatusFilter(''); setStateFilter(''); }}              className="text-sm text-slate-500 hover:text-slate-700 px-3 py-2 rounded-lg border border-slate-200 hover:bg-slate-50"            >              Clear            </button>          )}        </div>      )}      {/* States */}      {isLoading && <CasesTableSkeleton />}      {isError && (        <div className="flex items-center justify-center py-24">          <div className="text-center">            <div className="inline-flex items-center justify-center w-12 h-12 rounded-full bg-red-50 mb-3">              <svg width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="#ef4444" strokeWidth={1.75}>                <path strokeLinecap="round" strokeLinejoin="round" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />              </svg>            </div>            <p className="text-sm text-slate-600">Failed to load {vocab.matter_plural.toLowerCase()}.</p>          </div>        </div>      )}      {matters && matters.length === 0 && (        <div className="flex items-center justify-center py-24">          <div className="text-center">            <div className="inline-flex items-center justify-center w-14 h-14 rounded-2xl bg-slate-100 mb-4">              <svg width="24" height="24" fill="none" viewBox="0 0 24 24" stroke="#94a3b8" strokeWidth={1.5}>                <path strokeLinecap="round" strokeLinejoin="round" d="M20 7H4a2 2 0 00-2 2v10a2 2 0 002 2h16a2 2 0 002-2V9a2 2 0 00-2-2z" />                <path strokeLinecap="round" strokeLinejoin="round" d="M16 7V5a2 2 0 00-2-2h-4a2 2 0 00-2 2v2" />              </svg>            </div>            <h3 className="text-sm font-semibold text-slate-800 mb-1">No {vocab.matter_plural.toLowerCase()} yet</h3>            <p className="text-sm text-slate-500 mb-4">Get started by creating your first {vocab.matter_label.toLowerCase()}.</p>            <Link              to="/cases/new"              className="inline-flex items-center gap-2 rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white hover:bg-indigo-700"            >              <svg width="12" height="12" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2.5}>                <path strokeLinecap="round" strokeLinejoin="round" d="M12 4v16m8-8H4" />              </svg>              Create {vocab.matter_label.toLowerCase()}            </Link>          </div>        </div>      )}      {matters && matters.length > 0 && filtered.length === 0 && (        <div className="text-center py-16 text-slate-400 text-sm">          No {vocab.matter_plural.toLowerCase()} match your search.        </div>      )}      {filtered.length > 0 && (        <div className="rounded-xl border border-slate-200 bg-white overflow-hidden shadow-sm">          <table className="w-full text-left">            <thead>              <tr className="border-b border-slate-200 bg-slate-50/80">                <th className="px-5 py-3 text-xs font-semibold text-slate-500 uppercase tracking-wider">Ref</th>                <th className="px-5 py-3 text-xs font-semibold text-slate-500 uppercase tracking-wider">Title</th>                <th className="px-5 py-3 text-xs font-semibold text-slate-500 uppercase tracking-wider">{vocab.participant_label}</th>                <th className="px-5 py-3 text-xs font-semibold text-slate-500 uppercase tracking-wider">Status</th>                <th className="px-5 py-3 text-xs font-semibold text-slate-500 uppercase tracking-wider">Filed</th>                <th className="px-5 py-3 w-10" />              </tr>            </thead>            <tbody className="divide-y divide-slate-100">              {filtered.map((matter) => (                <MatterRow key={matter.id} matter={matter} vocab={vocab} />              ))}            </tbody>          </table>          {filtered.length < (matters?.length ?? 0) && (            <div className="border-t border-slate-100 px-5 py-2.5 text-xs text-slate-400">              Showing {filtered.length} of {matters?.length} {vocab.matter_plural.toLowerCase()} (filtered)            </div>          )}          {!search && !statusFilter && totalPages > 1 && (            <div className="border-t border-slate-100 px-5 py-3 flex items-center justify-between">              <p className="text-xs text-slate-400">Page {page} of {totalPages} &middot; {total} total</p>              <div className="flex items-center gap-2">                <button                  onClick={() => setPage((p) => Math.max(1, p - 1))}                  disabled={page === 1}                  className="px-3 py-1.5 text-xs rounded-lg border border-slate-200 text-slate-600 hover:bg-slate-50 disabled:opacity-40 disabled:cursor-not-allowed transition-colors"                >                  ← Prev                </button>                <button                  onClick={() => setPage((p) => Math.min(totalPages, p + 1))}                  disabled={page === totalPages}                  className="px-3 py-1.5 text-xs rounded-lg border border-slate-200 text-slate-600 hover:bg-slate-50 disabled:opacity-40 disabled:cursor-not-allowed transition-colors"                >                  Next →                </button>              </div>            </div>          )}        </div>      )}    </div>  );}
